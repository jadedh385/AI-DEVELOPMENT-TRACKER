# Decision Log 0002 — Stage 1.0 Scaffold

Date: 2026-07-06

Locked via the Stage 1.0 Preference Gate (before any code):

| Decision | Choice | Rationale |
|---|---|---|
| Test runner | **Vitest** | Fast, native ESM/TypeScript, minimal config, first-class Next.js support; quick red-green TDD cycles. |
| Package manager | **npm** | Ships with Node, zero extra install, most universally host/tutorial-supported. Reversible — switching later doesn't touch app code, DB, or any Phase 2 migration. |
| Schema scope (Phase 1) | **Source + Item + Feedback only** | YAGNI for a single-user MVP. `User` / `CandidateSource` deferred to Phase 2; Prisma makes adding tables later trivial. `qualityScore` + feedback fields exist from day one so signals accrue. |
| CI timing | **Defer to Stage 1.9** | Local-first, no GitHub remote yet. Stage 1.0 gets lint/typecheck/test npm scripts + a git pre-commit hook; GitHub Actions added at 1.9. |

Carried forward (locked earlier, see `0001-phase1-kickoff.md`): Next.js (App Router) + TypeScript + Tailwind + shadcn/ui, SQLite via Prisma, sources = Reddit + Hacker News + lab-blog RSS, once-daily refresh, local-first, name = "AI Landscape Signal".

Awaiting **GATE 1** approval on `docs/prd/phase-1-prd.md` + `docs/architecture/data-model.md` before writing scaffold code.
